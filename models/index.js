const UserModel = require('./User')
const QuestionModel = require('./Question')
const SubmitModel = require('./Submit')
const SubmitCodeModel = require('./submitCode')
const CheckModel = require('./questionCheck')

module.exports = {
    UserModel,
    QuestionModel,
    SubmitModel,
    SubmitCodeModel,
    CheckModel
}
